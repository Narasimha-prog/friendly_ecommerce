services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: ecommerce
      MYSQL_USER: lnreddy
      MYSQL_PASSWORD: lnreddy
      MYSQL_ROOT_PASSWORD: lnreddy
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
